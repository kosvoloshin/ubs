extends ../template

block variables
    - var title = 'Login'

block content
    .wrapper
        .login
            .login__block
                .login__logo
                    //- img(src="" alt="logo")
                form.login__form
                    input.login__input(type="text" placeholder="username")
                    input.login__input(type="password" placeholder="password")
                    button.login__button(type="submit") continue